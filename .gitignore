/AGENTS.md

# Nix build result links.
/result
/result-*

# Rime generates these at runtime; they contain caches, synchronization state,
# and learned user dictionaries rather than reproducible configuration.
**/rime/build/
**/rime/sync/
**/*.userdb/
**/rime/installation.yaml
**/rime/user.yaml
